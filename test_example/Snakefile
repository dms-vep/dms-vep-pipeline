"""Top-level ``snakemake`` file that runs analysis."""


import os


configfile: "config.yaml"


# include `dms-vep-pipeline` Snakefile
include: os.path.join(config["pipeline_path"], "Snakefile")


rule all:
    input:
        variant_count_files,
        prob_escape_files,
